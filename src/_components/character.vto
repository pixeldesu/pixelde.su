{{ comp ui.raisedCard { href: character.url, classes: `flex items-end bg-[url('${character.cover}')] bg-center bg-cover bg-no-repeat h-[200px] md:h-[${ character.featured ? "150" : "100"}px] !p-0 ${ character.featured ? "md:col-span-2" : ""}`, attributes: { target: '_blank' } } }}
  <div class="w-full px-3 py-1 bg-white/75 dark:bg-neutral-800/75 uppercase italic font-bold">{{ character.nickname }}</div>
{{ /comp }}